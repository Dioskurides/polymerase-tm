{% set name = "polymerase-tm" %}
{% set version = "2.1.3" %}
{% set python_min = "3.9" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/p/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 77cdb990a36b3399da9f3ec4f1249afcd962dbefd9b435494fe1f37d89f17ee9

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - polymerase-tm = polymerase_tm.cli:main

requirements:
  host:
    - python {{ python_min }}
    - pip
    - hatchling
  run:
    - python >={{ python_min }}
    - biopython >=1.80
    - matplotlib-base >=3.5
    - seaborn >=0.12
    - numpy >=1.20

test:
  requires:
    - pip
    - python {{ python_min }}
  imports:
    - polymerase_tm
  commands:
    - polymerase-tm --help
    - polymerase-tm ATGTCCCTGCTCTTCTCTCGATGCAA
    - pip check

about:
  home: https://github.com/Dioskurides/polymerase-tm
  summary: Exact reproduction of the NEB Tm Calculator for PCR primer analysis
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - Dioskurides
